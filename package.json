{
  "name": "knape-dashboard",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "workspaces": [
    "apps/api",
    "apps/app",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "bun run --filter @nuraview/api dev",
    "dev:app": "bun run --filter @nuraview/app dev",
    "dev:pm": "bun run --filter @nuraview/api dev & bun run --filter @nuraview/app dev",
    "pm:migrate": "bun run --filter @nuraview/api db:migrate",
    "build:packages": "bun run --filter @nuraview/permissions build && bun run --filter @nuraview/email build && bun run --filter @nuraview/mcp build",
    "pm:seed": "bun run --filter @nuraview/api seed",
    "dev": "bun run --filter @nuraview/api dev & bun run --filter @nuraview/app dev",
    "build": "bun run build:packages && bun run --filter @nuraview/api build && bun run --filter @nuraview/app build"
  },
  "trustedDependencies": [
    "@parcel/watcher",
    "@swc/core",
    "canvas",
    "esbuild",
    "msgpackr-extract",
    "msw",
    "sharp",
    "unrs-resolver"
  ],
  "dependencies": {
    "dotenv-mono": "^1.5.1"
  },
  "devDependencies": {
    "turbo": "^2.10.5",
    "typescript": "5.8.3",
    "esbuild": "0.28.1"
  }
}
