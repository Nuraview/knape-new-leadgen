# browser-use needs an LLM as its "brain". This project defaults to Google
# Gemini so it can reuse the CRM's existing key. Get one: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional alternatives — swap the llm in main.py to use these instead:
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
