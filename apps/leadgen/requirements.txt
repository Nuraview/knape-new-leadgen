# Install: python3 -m pip install -r requirements.txt
# If you see "externally-managed-environment" (PEP 668), use a venv
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# or: python3 -m pip install --break-system-packages -r requirements.txt
python-dotenv>=1.0.0
apify-client>=2.5.0,<3  # 3.x made .call() return a non-subscriptable Run; code uses run["defaultDatasetId"]
fastapi>=0.115.0
uvicorn>=0.30.0
psycopg[binary]>=3.2  # Postgres driver (cockpit + Milestone 2 storage)
reportlab>=4.0
